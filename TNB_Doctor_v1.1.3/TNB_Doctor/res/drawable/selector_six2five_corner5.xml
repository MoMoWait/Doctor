<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_selected="true"><shape android:shape="rectangle">

            <!-- 矩形的圆角半径 -->
            <corners android:radius="5dp" />
            <!-- 填充颜色 -->
            <solid android:color="@color/text_gray"></solid>

            <stroke android:width="1dp" android:color="@color/text_gray" />
        </shape></item>
    <item android:state_focused="true"><shape android:shape="rectangle">

            <!-- 矩形的圆角半径 -->
            <corners android:radius="5dp" />
            <!-- 填充颜色 -->
            <solid android:color="@color/text_gray"></solid>

            <stroke android:width="1dp" android:color="@color/text_gray" />
        </shape></item>
    <item android:state_pressed="true"><shape android:shape="rectangle">

            <!-- 矩形的圆角半径 -->
            <corners android:radius="5dp" />
            <!-- 填充颜色 -->
            <solid android:color="@color/text_gray"></solid>

            <stroke android:width="1dp" android:color="@color/text_gray" />
        </shape></item>
    <item><shape android:shape="rectangle">

            <!-- 矩形的圆角半径 -->
            <corners android:radius="5dp" />
            <!-- 填充颜色 -->
            <solid android:color="#ff5b32"></solid>

            <stroke android:width="1dp" android:color="#ff5b32" />
        </shape></item>

</selector>