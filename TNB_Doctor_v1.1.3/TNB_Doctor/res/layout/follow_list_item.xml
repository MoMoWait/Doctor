<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:padding="10.0dip" >

    <TextView
        android:id="@+id/follow_item_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#333333"
        android:paddingRight="20dp"
        android:textSize="18sp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:orientation="horizontal"
        android:paddingTop="6dp"
        android:gravity="center_vertical" >

        <TextView
            android:id="@+id/follow_item_state"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/task_bg_gray"
            android:gravity="center"
            android:padding="4dip"
            android:textColor="@color/follow_no_repeat"
            android:textSize="9.0dip" />

        <ImageView
            android:id="@+id/follow_item_arrow"
            android:layout_width="10dp"
            android:layout_height="16dp" 
            android:layout_marginLeft="8dp"
            android:contentDescription="@string/app_name"
            android:background="@drawable/collect_right">
        </ImageView>
    </LinearLayout>

    <TextView
        android:id="@+id/follow_item_timeanddoc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/follow_item_type"
        android:textColor="#999999"
        android:textSize="12sp" />

</RelativeLayout>